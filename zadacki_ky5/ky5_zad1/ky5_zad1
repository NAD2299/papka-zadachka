def page_digits(pages):
    if pages < 0:
        raise ValueError
    if type(pages) not in [int]:
        raise TypeError
    len_i = 0
    for i in range(1, pages + 1):
        len_i += len(str(i))
    return len_i
